import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  container: { padding: 10, backgroundColor: '#ff00a1', flexDirection: 'row', justifyContent: 'space-between' }
})

export default styles
